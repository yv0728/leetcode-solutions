function fn(arr)
    local ans = 0
    local left = 1
    local right = #arr

    while left < right do
        -- TODO: logic with left and right
        if CONDITION then
            left = left + 1
        else
            right = right - 1
        end
    end

    return ans
end
