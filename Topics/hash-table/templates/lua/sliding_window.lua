function fn(arr)
    local n = #arr
    local window = 0
    local left = 1
    local ans = 0

    for right = 1, n do
        -- TODO: add arr[right] to window

        while WINDOW_CONDITION_BROKEN do
            -- TODO: remove arr[left] from window
            left = left + 1
        end

        -- TODO: update ans
    end

    return ans
end
